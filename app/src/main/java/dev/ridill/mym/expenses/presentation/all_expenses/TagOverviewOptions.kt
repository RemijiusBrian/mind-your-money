package dev.ridill.mym.expenses.presentation.all_expenses

import androidx.annotation.StringRes
import dev.ridill.mym.R

enum class TagOverviewOptions(
    @StringRes val label: Int
) {
    DELETE(R.string.delete)
}